<!doctype html>
<title>Upload</title>
<h2>Upload submissions file (.csv)</h2>
<p>
  {% with messages = get_flashed_messages() %}
      {% if messages %}
        <ul class=flashes>
        {% for message in messages %}
          <li>{{ message }}</li>
        {% endfor %}
        </ul>
      {% endif %}
    {% endwith %}
</p>
<form method="post" name="sform" action="/display" enctype="mulitpart/form-data">
  <dl>
    <p>
      <input type="file" name="sfile" autocomplete="off" required>
    </p>
  </dl>
